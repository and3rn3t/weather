import React from 'react';

// Legacy barrel removed. This file now only exports a component to satisfy
// react-refresh/only-export-components. Prefer importing from
// './LoadingProvider' and './LoadingStateManager'.

const LoadingManagerComponent: React.FC = () => null;

export default LoadingManagerComponent;
